<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mylayouts"
        tools:targetApi="31">

        <!-- Screen 8 -->
        <activity
            android:name=".Screen8Activity"
            android:exported="false" />

        <!-- Screen 7 -->
        <activity
            android:name=".Screen7Activity"
            android:exported="false" />

        <!-- Screen 6 -->
        <activity
            android:name=".Screen6Activity"
            android:exported="false" />

        <!-- Screen 5 -->
        <activity
            android:name=".Screen5Activity"
            android:exported="false" />

        <!-- Screen 4 -->
        <activity
            android:name=".Screen4Activity"
            android:exported="false" />

        <!-- Screen 3 -->
        <activity
            android:name=".Screen3Activity"
            android:exported="false" />

        <!-- Screen 2 -->
        <activity
            android:name=".Screen2Activity"
            android:exported="false" />

        <!-- Screen 1 (halaman pertama saat app dibuka) -->
        <activity
            android:name=".Screen1Activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity (tidak dijalankan pertama kali) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>
